# ربط اللوحة ببوابة البيانات المفتوحة

اللوحة تقرأ من مصدرين معاً:

1. **ملف الإكسل** في `data/` — الأساس، ويعمل دائماً.
2. **البيانات الحية** من [بوابة البيانات المفتوحة](https://open.data.gov.sa) — تُجلب
   وتُخزَّن في `data/live/`، ثم تُدمج فوق الإكسل عند العرض.

إن تعذّر الوصول للبوابة، تبقى اللوحة تعمل من الإكسل دون أي انقطاع.

---

## الطريقة التلقائية (المُوصى بها)

اضغط مرتين على **`sync_and_push.bat`**. هذا كل شيء.

الملف يقوم بالتالي بنفسه:

1. يبحث في البوابة عن كل مؤشر غير مضبوط، باستخدام اسمه العربي.
2. يجرّب نتائج البحث واحدة واحدة: ينزّل الملف، يقرأه، **يكتشف عمود التاريخ
   تلقائياً**، ويتحقق أن أعمدته تطابق ورقة اللوحة.
3. يعتمد أول نتيجة تنجح ويحفظ إعداداتها في `sources.json`.
4. يحدّث البيانات ويرفعها للمستودع.

ما يفشل تلقائياً يُعرض باسمه وسببه، ويمكن ضبطه يدوياً بالطرق أدناه.
للتشغيل مرة واحدة بدون رفع: `python sync_data.py auto`

إذا لم يجد شيئاً، شغّل التشخيص وأرسل ناتجه لطلب المساعدة:

```bash
python sync_data.py diagnose > تقرير.txt
```

---

## الضبط اليدوي (عند فشل التلقائي)

لكل مؤشر في `sources.json` نحتاج تحديد من أين يُجلب. الأسهل هو **رابط الملف المباشر**.

### الطريقة الأولى: رابط ملف مباشر (الأكثر ثباتاً)

افتح صفحة المجموعة في البوابة، انسخ رابط تنزيل ملف CSV أو XLSX، ثم:

```jsonc
{
  "sheet": "GDP_Data",
  "enabled": true,
  "resource_url": "https://open.data.gov.sa/.../gdp.csv",
  "date_column": "الفترة"
}
```

### الطريقة الثانية: عبر معرّف المجموعة

```bash
python sync_data.py discover "الناتج المحلي الإجمالي"   # يعطيك dataset_id
python sync_data.py inspect <dataset_id>                # يعرض ملفات المجموعة
```

ثم ضع `dataset_id` في `sources.json` واجعل `enabled` تساوي `true`.

### مطابقة الأعمدة

أعمدة ملف البوابة غالباً لا تطابق أعمدة الإكسل. لمعرفة الفرق:

```bash
python sync_data.py preview <dataset_id|url> --sheet GDP_Data
```

يطبع أعمدة المصدر، وأعمدة الورقة الناقصة. أضف الفروق في `column_map`:

```jsonc
"column_map": {
  "الفترة": "Date",
  "GDP at current prices": "الناتج المحلي الاجمالي"
}
```

**مهم:** إن لم تتطابق الأعمدة، يُرفض المصدر ويُسجَّل الخطأ في صفحة «مصادر البيانات»،
ولا يصل إلى الرسوم. هذا مقصود — بيانات خاطئة أسوأ من بيانات قديمة.

### مفتاح الـ API (إن طلبته البوابة)

```toml
# .streamlit/secrets.toml  (غير مرفوع للمستودع)
open_data_api_key = "..."
```

أو متغيّر بيئة `OPEN_DATA_API_KEY`. وفي GitHub: Settings → Secrets → `OPEN_DATA_API_KEY`.

---

## التشغيل

```bash
python sync_data.py sync              # تحديث كل المصادر المُفعّلة
python sync_data.py sync --only cpi   # مؤشر واحد
python sync_data.py status            # حالة كل مصدر
```

ومن داخل اللوحة: زر **«تحديث البيانات»** في الشريط الجانبي، أو صفحة **«مصادر البيانات»**.

## التحديث التلقائي

**قيد مهم تم التحقق منه عملياً:** البوابة لا تستقبل اتصالات من خوادم خارج
السعودية. فحصٌ نُفِّذ على خوادم GitHub انتهى بـ `ConnectTimeout` على كل
المسارات ولكلا النطاقين (`open.data.gov.sa` و`od.data.gov.sa`) — الاتصال لا
يُفتح أصلاً. لذلك **لا يمكن جدولة التحديث على GitHub Actions العامة**،
وجدولة cron في `sync-data.yml` معطّلة عمداً.

المزامنة يجب أن تعمل من جهاز يصل للبوابة (داخل السعودية). طريقتان:

### الطريقة الأولى: جدولة على جهازك (الموصى بها)

**قبل أي شيء**، تأكد أن جهازك يصل للمصادر:

```bash
python sync_data.py diagnose
```

إن نجح، سجّل المهمة بضغطة واحدة: شغّل **`install_schedule.bat`** مرة واحدة.
يسجّل مهمة أسبوعية (الأحد ٨ صباحاً) مع تفعيل خيار «شغّل عند أول فرصة إذا
فاتت»، فلو كان الجهاز مغلقاً وقت الموعد تعمل عند تشغيله.

**لماذا أسبوعياً وليس يومياً؟** البيانات شهرية وربعية بطبيعتها (الناتج
المحلي ربعي، التضخم وساما شهرياً)، فالتحديث الأسبوعي يسبق صدور أي رقم جديد
بفارق مريح، ويقلّل الحمل على مصادر البيانات.

للتشغيل يدوياً في أي وقت: اضغط `sync_and_push.bat` مرتين.
لحذف المهمة: `schtasks /Delete /TN "تحديث لوحة المؤشرات" /F`

### قبل شراء أي استضافة — انتبه

الدليل المتوفر أن خوادم GitHub الأمريكية لا تصل للمصادر. لكن كثيراً من
المواقع الحكومية تحجب عناوين مراكز البيانات عموماً لا الجغرافيا وحدها،
ما يعني أن سيرفراً سحابياً — حتى داخل السعودية — قد يُحجب أيضاً بينما
يعمل الاتصال المنزلي. لذلك لا تعتمد على استضافة قبل التأكد من نجاح
`diagnose` على تلك الاستضافة نفسها.

### الطريقة الثانية: self-hosted runner

أضف runner على جهاز داخل السعودية
(Settings → Actions → Runners → New self-hosted runner)، ثم في
`sync-data.yml` أعد تفعيل كتلة `schedule` وغيّر `runs-on` إلى `self-hosted`.

### تشغيل يدوي

من تبويب Actions → Sync open data → Run workflow. (سينجح فقط مع
self-hosted runner، وسيفشل على خوادم GitHub العامة للسبب أعلاه.)

## كيف يتم الدمج

| `merge_mode` | السلوك |
|---|---|
| `extend` (الافتراضي) | يبقي تاريخ الإكسل كاملاً، ويضيف الفترات الجديدة. عند تكرار فترة، تفوز بيانات البوابة. |
| `replace` | يستبدل الورقة بالكامل ببيانات البوابة. |

## أين تعمل اللوحة نفسها؟

اللوحة تقرأ من `data/live/` المحفوظ في المستودع، فهي تعمل في أي مكان —
بما فيه Streamlit Cloud — حتى لو كان الخادم لا يصل للبوابة. الوصول للبوابة
مطلوب فقط لحظة **المزامنة**، لا لحظة **العرض**.

انتبه: زر «تحديث البيانات» داخل اللوحة يحاول الاتصال بالبوابة مباشرة، فهو
يعمل عند تشغيل اللوحة محلياً، ولن يعمل إذا كانت مستضافة خارج السعودية —
وفي تلك الحالة يبقى التحديث عبر `sync_and_push.bat` من جهازك.

## ملاحظة على تغطية المصادر

مؤشرات البنك المركزي السعودي (التمويل العقاري، القروض الاستهلاكية) ومؤشرات
التأمينات الاجتماعية قد لا تكون منشورة على البوابة الوطنية بنفس الشكل؛ إن لم تجدها
هناك، استخدم `resource_url` مع رابط الملف من موقع الجهة نفسها — الآلية نفسها تعمل
مع أي رابط ملف مباشر.
